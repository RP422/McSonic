doctype html
html
  head
    link(href='style.css', rel='stylesheet')
  body
    header
      h1 My Website!
      nav
        ul
          each item in config.menu
            li
              a(href=item[1]) #{item[0]}
    div(id='content')
      block content
    footer