extends layout

block content
    ul
        each item in config.menu["Sides"]
            li
                h3.itemName=item["name"]
                p.itemDescription=item["description"]
                img.itemIm(src=item["img_path"])